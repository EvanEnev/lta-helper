import {LTPayrollIssueData, LTWorker} from '@/src/utils/types'

interface PayrollIssuePageProps {
  payrolls: LTPayrollIssueData[]
  workers: LTWorker[]
}

export default function PayrollIssuePage({}) {
  return <main></main>
}

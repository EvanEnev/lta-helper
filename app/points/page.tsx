// import getPoints from '@/lib/functions/points/getPoints'
// import {DateTime, Interval} from 'luxon'

export default async function Points() {
  // const now = DateTime.now()
  // const interval = Interval.fromDateTimes(
  //   now.startOf('month'),
  //   now.endOf('month'),
  // )
  //
  // const pointsData = await getPoints({interval})
  return <main></main>
}
